# 实验阶段记录

## 第一期：固定提示词排列组合

### 思路

第一期把若干提示词当作完整原子，只生成它们的排列组合，再叠加固定分隔符和大小写变体。候选模型相当于：

```text
提示词子集 + 固定分隔符 + 大小写变体
```

### 结果

- 生成器只覆盖提示词排列，不能生成独立的自然数字模式。
- 数字提示只能作为完整字符串出现，不能按日期、年份、手机号片段或数字规律拆解。
- 初始脚本共生成 4689 个候选。
- 实验中验证过的第一期候选均未通过验证。

### 结论

第一期的候选空间假设过窄，失败不能通过继续增加分隔符解决，必须改变候选生成模型。

## 第二期：规则语法、分层评分和排除基线

### 思路

第二期把候选拆成“来源原子 + 规则变换 + 评分 + 长度阶段”：

- 文本原子：大小写、反转、截取、简单字符替换；
- 数字原子：完整数字、连续片段、倒序、去前导零、日期式重排；
- 自然数字：递增/递减、重复数字、重复块、常见数字模式；
- 励志短句：只生成无空格拼音，优先级低于个人提示词；
- 组合规则：文本、数字、拼音之间的有限前后缀和分隔符组合；
- 长度阶段：先输出 `1..15` 位，再输出 `16..20` 位；
- 去重：同一候选只保留一次；
- 排除：读取第一期 TXT，已验证失败的候选不再进入评分、排序或输出。

### 本次第二期新增

- 将四条作者个人线索放入本地忽略文件，不写入代码和公共文档；
- 对字母、数字、点号混合线索同时保留原样、去分隔符、拆分和重组形式；
- 对长数字线索保留完整值、前缀/后缀、片段和倒序形式；
- 新结果写入 `toy_candidates_v2.txt`，第一期基线保留为 `toy_candidates_v1.txt`；
- 默认输出上限为 50000 行。

### 结果记录

运行时以命令行统计为准，避免把个人线索或候选值写入本文件：

- 第一版基线：50000 行，作为第二期精确排除集合；
- 第二期输出：最多 50000 行；
- 每行一个候选值，ASCII-only，无中文、无空格；
- 生成器和排除逻辑由自动化测试覆盖。

### 局限

排除第一期只能减少重复，不会自动发现密码规则。如果密码是随机生成、来自未提供的个人语料，或验证对象与假设不一致，候选算法无法从空白信息中推导出正确值。此时应优先使用密码管理器、系统钥匙串、旧设备备份或官方恢复流程。
