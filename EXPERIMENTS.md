# DeCodeMoMo 实验记录

## 项目定位

DeCodeMoMo 是一个根据用户提示进行密码解密的实验项目。它不假设密码一定属于某种固定格式，而是把用户提供的线索转成多个候选生成规则，再通过本地 TXT 文件保存候选，供后续实验验证。

这里记录的是候选算法的构想、覆盖范围和实验结果，不把某一批候选误认为正确答案。

## 第一期：固定提示词排列组合

### 初始构想

第一期采用了一个很强的假设：密码就是若干提示词的排列组合。算法把每个提示词当成不可拆分的完整字符串，然后依次生成：

```text
提示词子集
    + 不同排列顺序
    + 固定分隔符
    + 大小写变体
```

例如，一个候选只能来自提示词本身、两个提示词拼接、多个提示词拼接，或这些结果的大小写和分隔符变化。

### 实验结果

- 初始脚本共生成 4689 个候选；
- 数字提示被当作完整字符串，不能拆成日期、年份或手机号片段；
- 算法没有生成与提示词无关的自然数字组合；
- 不能表达键盘误触、旧密码变体、拼音输入和个人短语等规则；
- 已经验证的第一期候选均不是正确密码。

### 结论

第一期失败不是候选数量不足，而是候选空间定义错误。继续增加分隔符、大小写或排列顺序，只会在同一个错误假设下重复搜索，不能覆盖真正的密码构造方式。

## 第二期：规则族、分层评分和失败排除

### 优化目标

第二期的目标是把“提示词排列器”改成“线索规则实验器”：同一条线索可以有多个解释，候选需要按来源可信度和规则复杂度排序，而不是按输入顺序机械输出。

### 候选规则

第二期将线索拆成以下规则族：

1. **个人提示词**

   保留原文，并生成大小写、反转、截取和简单字符替换。用户明确提供的个人线索优先级最高。

2. **混合线索**

   对字母、数字和分隔符混合的线索同时保留原样、去分隔符、拆分后的片段和按原顺序重组的形式。

3. **数字和日期**

   生成完整数字、前缀、后缀、连续片段、倒序、去前导零，以及有限的日期和年份排列。

4. **励志短句拼音**

   公开短句只作为低优先级来源，转换成无空格、无声调的小写拼音。它们不能排在用户个人提示词之前。

5. **自然数字模式**

   覆盖递增、递减、重复数字和重复块，但统一使用较低权重，避免大量通用数字占据搜索预算。

### 排序和长度策略

候选先按长度阶段分组，再按来源层级和评分排序：

```text
第一阶段：1..15 位
第二阶段：16..20 位
```

来源优先级为：

```text
个人提示词
    > 个人提示词的简单变体
    > 个人线索之间的组合
    > 公开励志短句拼音
    > 通用纯数字模式
```

每个候选在内部携带来源、规则和评分，但 TXT 只写候选字符串。

### 第一版失败候选排除

第二期读取第一期 TXT，建立精确排除集合。候选在进入评分、排序和输出前就进行过滤，因此：

- 第一版已经验证失败的候选不会再次生成到输出文件；
- 第二期输出写入独立的 `toy_candidates_v2.txt`；
- 第一版基线保留为 `toy_candidates_v1.txt`；
- 多次实验可以继续叠加多个排除 TXT。

### 当前实验结果

- 第一版基线：50000 条候选；
- 第二期使用新增个人线索和第一版排除集合；
- 第二期输出上限：50000 条；
- 当前输出每行一个候选，ASCII-only，不含中文；励志短句拼音本身不含空格，其他组合仍可保留规则允许的分隔符；
- 第二期候选与第一期基线没有交集；
- 16 项自动化测试全部通过。

当前结果只说明第二期完成了候选空间和实验流程的优化，不代表已经找到正确密码。后续验证结果应继续记录为新的实验数据，而不是直接修改历史结论。

## 第三期：意义约束和顺序约束

### 新增实验提示

作者补充说明：密码不会使用没有意义的数字或字符组合，也不会把给出的关键字翻转。同时新增了两条个人关键字线索；它们只保存在本地提示文件中，不写入公共文档或代码。

这意味着第二期虽然扩大了覆盖范围，但其中大量通用数字、反转字符串和无来源拼接已经与新的作者提示冲突。第三期的目标不再是继续扩张候选数量，而是提高每个候选与已知信息的一致性。

### 第三期生成规则

第三期只保留可以解释来源的变换：

1. 提示原文及大小写变体；
2. 混合提示去掉原有分隔符后的紧凑形式；
3. 混合提示中长度不少于两位的原始字母或数字部分；
4. 完整数字提示，以及可识别的年份和有效日期；
5. 相邻提示按输入顺序直接连接，最多连接三个提示；
6. 无空格、无声调的励志短句拼音，作为最低优先级的有意义文本来源。

第三期明确取消：

- 所有文本、数字、混合提示和最终候选的反转；
- 通用数字、重复数字、重复块及递增递减序列；
- 任意数字切片、字符替换和人为添加的分隔符；
- 改变提示顺序的排列组合；
- 拼音短句与个人提示之间的无边界笛卡尔积。

### 失败候选排除

第三期把以下数据合并为精确排除集合：

```text
第一期 TXT 的前 10101 条
    + 第二期 TXT 的前 10101 条
```

这里只排除每个文件的前 10101 条，不会把第二期尚未验证的剩余候选误标记为失败。排除在排序和写出之前完成，因此 `toy_candidates_v3.txt` 与两个已验证前缀没有交集。

### 排序和输出

第三期仍采用两阶段长度排序：

```text
第一阶段：1..15 位
第二阶段：16..20 位
```

同一长度阶段内，来源优先级为：

```text
个人提示及其直接变体
    > 相邻提示的有序组合
    > 公开励志短句拼音
```

第三期结果独立写入 `toy_candidates_v3.txt`，每行一个候选，不包含中文、评分或规则说明。生成文件和本地个人提示继续由 `.gitignore` 排除，不上传到 GitHub。

### 当前第三期结果

- v3 已生成 73 条候选；
- 所有候选长度均不超过 20 位，并按 `1..15` 位、`16..20` 位分阶段排列；
- v3 不包含通用纯数字模式或反转形式；
- v3 与 v1/v2 各自前 10101 条排除前缀没有交集；
- 25 项自动化测试全部通过。

这只说明候选规则和排除流程符合第三期假设，不代表已经找到正确密码。后续验证结果应作为新的实验数据记录。

## 第四期：重复语义片段和跨位置组合

### 第三期失败后的修正

第三期的“仅按输入顺序连接相邻提示”规则过于保守。它虽然排除了随机排列，却也错误地阻止了相距较远、但同样来自明确线索的关键字组合。

第四期区分两个概念：

- 不翻转关键字，是指不改变关键字内部字符顺序；
- 组合关键字，是指多个完整语义原子可以采用不同前后顺序连接。

因此，第四期允许两个语义原子双向组合，但仍不会把单个原子倒序。

### 重复片段识别

如果同一个连续片段在至少两条不同提示中出现，它比任意数字切片更可能具有真实含义。第四期寻找长度至少三位的最长公共片段，并只保留以下两类结果：

1. 含数字的重复片段；
2. 在至少一条提示中本身就是完整词元的字母片段。

第二条约束用于防止不同拼音读法产生无意义的公共后缀。公共片段只保留最长形式，其内部较短子串不会重复进入候选池。

### 第四期组合语法

候选来源按以下方式扩展：

1. 个人提示原文及大小写变体；
2. 混合提示的原文、紧凑形式及有意义完整部分；
3. 跨提示发现的最长重复语义片段；
4. 任意两个来源不冲突的语义原子，按两种顺序直接连接；
5. 最多三个不冲突的原始语义原子进行有限排列；
6. 公开励志短句拼音仍作为低优先级独立候选，不参加无边界组合。

所有组合都不插入无来源字符或分隔符，也不翻转原子内部字符。

### 精确排除规则

第四期默认排除：

```text
第一期 TXT 的前 10101 条
    + 第二期 TXT 的前 10101 条
    + 第三期 TXT 的全部 73 条
```

排除只匹配完整候选字符串。一个单独原子已经验证失败，只会排除该原子本身，不会排除包含它的新组合。

### 当前第四期结果

- 新增中文关键字已转换为无空格、无声调拼音，只保存在本地提示文件；
- v4 已生成 463 条互不重复的候选；
- 所有候选长度不超过 20 位，并按 `1..15` 位、`16..20` 位分阶段排列；
- v4 与 v1/v2 各自前 10101 条及完整 v3 均无交集；
- 目标跨位置组合已经进入 v4；
- 自动化测试全部通过。

第四期结果写入 `toy_candidates_v4.txt`。生成文件和个人提示继续保留在本地，不上传到 GitHub。

## 第五期：作者画像与排序优先

### 思路转折

前四期都把实验当成“候选空间覆盖问题”，每失败一次就换一批构造规则。这个方向从一开始就缺少可验证的中间反馈：每一期只能在全部猜完之后得到一个“都不对”的结论，无法判断是规则方向错了，还是排序把正确答案埋在了第 800 条。

第五期接受了一个更贴近实际的定位：这是一个**只能验证终值、但尝试次数不限**的搜索问题。尝试次数不受限时，真正决定实验成本的不是“能不能生成出正确候选”，而是**正确候选排在第几位**。于是目标从“扩大规则族”改为“把候选按可信度排序”。

另一个关键变化是不再假设“密码是提示词的排列组合”。作者给出的线索只回答一个问题——**他自己会怎么设密码**；线索之间如何组合，由人生线索的可信度决定，而不是由字符串算子决定。

### 作者画像

线索被整理成一份分类画像 JSON：

```text
author_profile.example.json   仓库内的脱敏模板
author_profile.local.json     本地真实画像，已被 .gitignore 忽略
```

真实画像只保存在本地，不进入仓库；新增中文关键字在画像中只以无空格拼音保存，中文原文只写在本地文件里。画像结构为“分类 → 条目”，分类带默认权重，条目可以单独覆盖权重：

| 分类 | 权重 | 内容 |
| --- | --- | --- |
| identity | 100 | 姓名拼音、姓名首字母 |
| history | 96 | 曾用密码 |
| aliases | 95 | 姓名形态之间的对应关系（只描述关系，不产生候选） |
| online_handle | 90 | 网名、笔名、《将进酒》类昵称 |
| dates | 86 | 生日、年份 |
| games | 78 | 游戏名与角色名（暂空，用户确认作者不会用） |
| zodiac | 76 | 星座拼音 |
| interests | 74 | 爱好（暂空） |
| works | 72 | 诗词、书名、台词 |
| numbers | 70 | 个人数字 |
| contacts | 66 | 电话与号码片段（其中出现在曾用密码里的片段单独提高为 92） |
| relationships | 62 | 家人、宠物（暂空） |
| places | 58 | 家乡、学校、公司（暂空） |
| inspiration | 尾部分类 | 励志短句拼音，不参与组合，且永远排在提示词之后 |

暂空分类只保留结构，等线索确认后再回填；不确定的线索不写入画像。

### 候选语法

候选由完整原子拼接而成，遵守用户给出的硬约束：

1. 不反转任何字符串；
2. 不拆分已经给出的密码或关键字（带小数点的曾用密码只能整体出现，不能派生出它的字母段或数字段）；
3. 大小写只有三种形态：原样、词首大写、整体大写，绝不制造词内大写；
4. 组合只做整块直接连接，不插入任何分隔符或新字符；
5. 不生成没有来源的自然数字或字符组合；
6. 单个原子、两原子（两种顺序）、三原子（仅限最高权重分类中的少量原子）。

### 排序公式

```text
分数 = 最弱原子权重 − 6 × (原子数 − 1) − 超过 15 位时的 20 分惩罚
```

三条设计意图：

- 取最弱原子，保证拼接结果不会超过它最强组成部分的可信度，例如“姓名缩写+号码片段”不会排到曾用密码之前；
- 每多拼一个原子扣 6 分，让“单一线索”优先于“两个线索的猜测”，再优先于三个线索；
- 用户判断密码大概率不超过 15 位，因此 16 位以上统一重罚，使同等可信度下 15 位以内的候选全部靠前，但极强线索仍能压过弱短候选。

长度不是硬分层，而是一个可调权重：换成硬分层只需把惩罚调大或改排序键。

### 失败基线排除

第五期默认排除：

```text
第一期 TXT 的前 10101 条
    + 第二期 TXT 的前 10101 条
    + 第三期 TXT 全部 73 条
    + 第四期 TXT 全部 463 条
```

排除按完整字符串匹配。一个单独原子被验证失败，只排除它本身，不会连带排除包含它的新组合。

### 当前第五期结果

- 新增线索（笔名、网名、星座、《将进酒》昵称、年份）已写入本地画像；
- 生成器 `profile_candidates.py` 输出 1451 条互不重复候选；
- 全部候选 ASCII-only，长度不超过 20 位，前 483 条为 15 位以内；
- 与 v1/v2 各自前 10101 条、完整 v3 与 v4 的交集均为 0；
- 三条曾用密码中，只有一条从未在以往任何一期的候选里出现过，它排在第一位；
- 51 项自动化测试全部通过。

第五期结果写入 `toy_candidates_v5.txt`，该文件、真实画像与本地提示继续由 `.gitignore` 排除，不上传到 GitHub。

只说明排序方向符合现有线索，不代表已经找到正确密码。

## 后续优化方向

## 第七期：单位置的手抖打错

### 假设来源

第六期仍然失败后，用户提出一个新的可能性：密码本身可能没有错，是**作者当年设置时多打了一个字母或数字**。这个假设的价值在于它不需要新线索，只需要对已经排好序的候选做单位置变异。

### 只覆盖三种手抖形态

第七期不做任意字符插入，只生成三种可以从“手抖”直接解释的形态：

1. **连击（stutter）**：某一位被按了两下，多出一个相同字符（`wsq` → `wssq`）；
2. **漏打（drop）**：某一位没有按下去（`wangsiqi` → `wangsiq`）；
3. **结尾多敲一个数字（append）**：`2580` → `25805`。

### 生效范围和排序

- 只对权重 ≥ 84 且长度 ≤ 12 的线索做变异，避免低可信线索把候选池撑大；
- 变异后的线索权重比原线索低 12 分，因此“线索本身”永远排在“线索打错版”之前；
- 变异线索可以继续与高可信短线索（权重 ≥ 84、长度 ≤ 8）双向拼接，因此 `wssq` + `0228` 这类形态也在覆盖范围内；
- 与 v6 一样不反转、不拆分、不加入无来源字符。

### 当前第七期结果

- 生成 15044 条互不重复的候选，全部 ASCII、长度不超过 20 位，其中 15 位以内 11685 条；
- 与 v1/v2 各自前 10101 条、完整 v3～v6 的交集均为 0；
- 61 项自动化测试全部通过。

第七期结果写入 `toy_candidates_v7.txt`，只保存在本地。

### 这个假设的固有弱点

大多数注册流程要求把密码输入两遍。若设置时手抖，作者必须用同样的方式手抖两次，密码才会被保存成打错的样子，因此“随机手抖”比“他本来就记错了拼写”要弱。真正更常见的是**稳定的拼写错误**：他始终以为自己的网名是某个拼法。因此后续应优先核对线索本身的拼写，而不是继续扩大字符变异。

## 后续优化方向

## 第六期：从作者旧密码反推书写习惯

### 第五期失败后的重新定位

第五期验证失败说明：把线索**整块拼接**（含大小写变体）这条路已经走完了。v5 覆盖了所有“1～2 个线索直接相连”的组合，因此失败的信息量不只是“这批不对”，而是“作者没有用简单的整块拼接”。

于是第五期遗漏的不是线索，而是**变换**：作者把线索写进密码时，到底做了什么处理。最可靠的证据不是任何外部常识，而是作者自己的旧密码。

### 旧密码暴露出的三条习惯

```text
wyy25805.0        → 字母开头 + 数字 + 符号 + 版本号式尾号
013713992580      → 手机号前补前导 0
168168            → 片段自我重复
```

第六期把这三条习惯变成规则，而不是继续扩大线索表：

1. **符号分隔**：高可信线索（权重 ≥ 84）之间的组合额外生成 `.` 和 `_` 两种分隔形式；空分隔仍然保留，并且排在符号形式之前。
2. **片段重复**：长度不超过 6 位的线索生成自我重复形式（如 `wsqwsq`、`25802580`），权重比原线索低 8 分。
3. **版本号递增**：形如 `xxx5.0` 的旧密码会生成 `xxx5.1`～`xxx5.9`、`xxx4.0`、`xxx6.0`，只改尾部版本号，不改动正文。

### 新增线索

第六期补充了三类可以直接解释来源的原子：

- 网名的首字母缩写（作者已有 `wyy`、`wsq` 的首字母习惯）；
- 出生年份 `2001`、星座英文写法；
- 网名出处《将进酒》的名句拼音片段。

### 评分参数外置

长度阈值、超长惩罚、组合惩罚、分隔符集合、分隔符门槛、重复惩罚、版本号范围全部移到画像的 `combination_rules` 中，每轮实验只改数据，不改代码。

### 当前第六期结果

- 生成 5868 条互不重复的候选，全部 ASCII、长度不超过 20 位，其中 15 位以内 3666 条；
- 与 v1/v2 各自前 10101 条、完整 v3/v4/v5 的交集均为 0；
- 55 项自动化测试全部通过。

第六期结果写入 `toy_candidates_v6.txt`，同样只保存在本地。

### 仍未纳入模型的方向

以下方向刻意没有生成，因为缺少目标侧信息，生成它们更像是消耗尝试次数：

- 更明显的字符替换（字母换数字或符号）；
- 站名、应用名作为前后缀；
- 中文原文字符密码；
- 键盘位移、输入法状态等输入错误类结果。

## 后续优化方向

第五期之后，每轮只需要补充“作者本人”这一条线索的侧面信息，然后重新排序：

- 昵称、游戏名、网名、论坛马甲；
- 学校、公司、常驻城市；
- 星座以外的出生信息，或日期对应的真实语义；
- 旧密码的构造习惯（版本号、数字后缀、符号位置）。

同时应避免继续扩大公开短句库：公开短句属于低可信度来源，只会把提示词候选往后挤。

如果第四期仍然失败，下一步不应继续无边界增加公开短句，而应优先检查：

- 密码是否来自旧密码变体；
- 是否存在键盘布局、Caps Lock 或相邻键误触；
- 日期对应的真实语义是否判断错误；
- 验证的对象是否确实是同一个密码字段；
- 密码是否本来就是随机生成且没有可推导线索。

如果密码属于随机生成类型，候选规则算法无法凭空恢复它，应转向密码管理器、系统钥匙串、旧设备备份或官方恢复流程。
